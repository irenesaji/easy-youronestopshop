# Flask web framework
Flask==3.0.0
flask-cors==4.0.0

# LangChain and Groq for AI chat
# langchain-groq requires langchain-core <0.3.0; pin langchain to a 0.2.x release
langchain-groq==0.1.9
langchain==0.2.17
# removed explicit langchain-core pin (caused dependency resolution conflicts with langchain-groq)
# let `langchain` / pip choose a compatible `langchain-core` version

# Environment variable management
python-dotenv==1.0.0

# Additional utilities
Werkzeug==3.0.1
